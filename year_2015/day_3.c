#include "libs/futil.h"
#include "libs/set.h"

int main()
{
  // FILE* f = open_file("inputs/day_3.txt", "r");

  // close_file(f);
  //  hash_map* map = new_map(1000);
  //  set_key(map, "key", true);
  //  printf("%d\n", search_in_map(map, "key"));

  return 0;
}